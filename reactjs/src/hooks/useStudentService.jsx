import {submitForm} from "../api/StudentService"
const useStudentService=()=>{
    const [isLoading, setIsLoading] = useState(false);
    const [error,setError]= useState();
    const submitRequestForm = submitForm();
    const getRequestForm =();
    const 
}